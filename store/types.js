const getters = {
  IS_TOUCH_DEVICE: 'IS_TOUCH_DEVICE',
};
const mutations = {
  SET_IS_TOUCH_DEVICE: 'SET_IS_TOUCH_DEVICE',
};
const actions = {

};

export const rootVuexTypes = Object.freeze({
  ...mutations,
  ...actions,
  ...getters,
});
